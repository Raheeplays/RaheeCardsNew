import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

export default defineConfig({
  plugins: [react()],
  // Repo ka naam exact hona chahiye
  base: '/RaheeCardsNew/', 
  build: {
    outDir: 'dist',
  }
})

